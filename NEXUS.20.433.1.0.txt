@0
-IN:JRO ANY
SUM:MOV ACC,LAST
    JMP RES
NXT:MOV 3,DOWN#NXT
RES:MOV 0,ACC
    JMP -IN
PRI:MOV 5,DOWN#GET
    MOV DOWN,RIGHT
    JMP -IN
BEG:MOV 1,DOWN#BEG
    JMP RES
INC:ADD 1

@1
BEG:MOV UP,ACC
    MOV 9,LEFT#BEG
STP:MOV ACC,DOWN
-IN:JRO DOWN
DIV:MOV 1,LEFT#SUM
    MOV LEFT,ACC
    MOV 6,LEFT#PRI
    MOV LEFT,RIGHT
    JMP STP
NDI:SUB 1
    JEZ END
    ADD 1
    MOV 3,LEFT#NXT
    JMP STP
END:MOV 0,RIGHT

@2
MOV LEFT,DOWN

@3
-IN:JRO ANY
BEG:MOV 2,ACC
    JMP -IN
NXT:ADD 1
    JMP -IN
GET:MOV ACC,LAST
    JMP -IN
GCN:MOV ACC,LAST
    MOV 11,UP#INC

@4
BEG:MOV UP,ACC
REM:MOV 7,LEFT#GCN
    SUB LEFT
    JGZ REM
    JEZ DIV
    MOV 6,UP#NDI
    JMP BEG
DIV:MOV 1,UP#DIV

@5
MOV UP,DOWN

@6


@7


@8
MOV RIGHT,DOWN

@9
MOV UP,LEFT

@10
