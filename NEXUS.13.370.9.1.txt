@0
BEG:MOV RIGHT,ACC
    JGZ 90
    MOV 1,RIGHT#RH
    JMP BEG
90: SUB 90
    JGZ 180
    MOV 3,RIGHT#UP
    JMP BEG
180:SUB 90
    JGZ 270
    MOV 5,RIGHT#LF
    JMP BEG
270:MOV 7,RIGHT#DW

@1
BEG:MOV UP,LEFT
-IN:JRO LEFT
RHT:MOV 8,DOWN#HRT
    JMP POS
UP: MOV 1,DOWN#VRT
    JMP NEG
LFT:MOV 8,DOWN#HRT
    JMP NEG
DWN:MOV 1,DOWN#VRT
POS:MOV UP,RIGHT
    JMP LEN
NEG:MOV UP,ACC
    NEG
    MOV ACC,RIGHT
LEN:MOV RIGHT,DOWN

@2
BEG:MOV LEFT,ACC
    JLZ NEG
    SUB 1
    MOV ACC,LEFT
    JMP BEG
NEG:ADD 1
    MOV ACC,LEFT

@3


@4
-IN:JRO ANY
VRT:MOV ACC,LAST
    SWP
    MOV ACC,LAST
    ADD LAST
    SWP
    JMP -IN
HRT:MOV ACC,LAST
    ADD LAST
    SWP
    MOV ACC,LAST
    SWP

@5
-IN:JRO UP
VRT:MOV 1,LEFT#VRT
    MOV LEFT,RIGHT
    MOV LEFT,RIGHT
    MOV UP,ACC
    MOV ACC,LEFT
    MOV ACC,RIGHT
    JMP -IN
HRT:MOV UP,ACC
    MOV ACC,DOWN
    MOV 7,LEFT#HRT
    MOV LEFT,DOWN
    MOV ACC,LEFT
    MOV LEFT,DOWN

@6
BEG:MOV 1,RIGHT#SX
    MOV LEFT,RIGHT
    MOV LEFT,RIGHT
    MOV LEFT,ACC
    JGZ CNT
    MOV 6,RIGHT#SU
    NEG
    MOV ACC,RIGHT
CNT:MOV 8,DOWN#PIX
    MOV 10,RIGHT#G
    MOV RIGHT,DOWN
    MOV RIGHT,DOWN
    SUB 1
    MOV ACC,DOWN
    JGZ CNT

@7
-IN:JRO ANY
SXY:MOV LAST,ACC
    SWP
    MOV LAST,ACC
    SWP
    JMP -IN
SUB:SWP
    SUB LAST
    SWP
    JMP -IN
GXY:MOV ACC,LAST
    SWP
    MOV ACC,LAST
    SWP

@8
BEG:MOV UP,ACC
    MOV 1,RIGHT#LI
    SAV
    JLZ NEG
    MOV UP,RIGHT
    MOV UP,RIGHT
    MOV ACC,RIGHT
    JMP BEG
NEG:ADD UP
    MOV ACC,RIGHT
    MOV UP,RIGHT
    SWP
    NEG
    MOV ACC,RIGHT

@9
-IN:JRO ANY
LIN:MOV LEFT,DOWN
    MOV LEFT,DOWN
    MOV LEFT,ACC
CNT:MOV 3,DOWN
    SUB 1
    JLZ END
    JMP CNT
PIX:MOV UP,DOWN
    MOV UP,ACC
    ADD UP
    MOV ACC,DOWN
    MOV 3,DOWN
END:MOV -1,DOWN

@10
